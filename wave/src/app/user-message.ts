export class UserMessage {
  userName: string;
  message: string;
  date: Date;

  constructor( userName: string, message: string, date: Date ) {
        this.userName = userName;
        this.message = name;
        this.date = date;
    }
}
